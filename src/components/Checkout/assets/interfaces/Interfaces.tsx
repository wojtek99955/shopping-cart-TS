export interface radioValues {
  picked: string;
}

export interface FormValues {
  email: string;
  firstName: string;
  newsletter: boolean;
  lastName: string;
  address: string;
  zip: string;
  city: string;
  country: string;
}
